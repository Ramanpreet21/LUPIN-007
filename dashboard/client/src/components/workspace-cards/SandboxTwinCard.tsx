import { Box, CheckCircle2, Cpu, HardDrive, LockKeyhole, Network, Play } from "lucide-react";
import type { SandboxTwinData, WorkspaceViewProps } from "@/types/workspace-cards";

export function SandboxTwinCard({ context, data, onAction, className = "" }: WorkspaceViewProps<SandboxTwinData>) {
  const cpuWidth = `${data.resourceLimits.cpuUsedPercent}%`; const memoryWidth = `${Math.round((data.resourceLimits.memoryUsedMb / data.resourceLimits.memoryCapMb) * 100)}%`;
  return <section className={`workspace-card sandbox-card ${className}`.trim()} aria-label="Sandbox twin inspector"><header className="workspace-card-head"><div><p className="eyebrow">{context.targetHostname} · isolated test surface</p><h2>Sandbox twin</h2><span>Trial execution remains sealed from the active production path.</span></div><span className="workspace-card-state"><CheckCircle2 size={13} />{data.state.replace("_", " ")}</span></header><div className="sandbox-layout"><section className="sandbox-identity"><span><Box size={22} /></span><div><p className="eyebrow">Rootless container</p><strong>{data.containerId}</strong><small>Podman twin · protected replication</small></div><button type="button" onClick={() => onAction?.("RUN_SANDBOX_TEST", { containerId: data.containerId })}><Play size={13} />Run test build</button></section><section className="sandbox-resources"><div><header><span><Cpu size={13} />CPU allocation</span><strong>{data.resourceLimits.cpuUsedPercent}%</strong></header><i><b style={{ width: cpuWidth }} /></i><small>{data.resourceLimits.cpuCapCores} vCPU cap · rootless execution</small></div><div><header><span><HardDrive size={13} />Memory allocation</span><strong>{data.resourceLimits.memoryUsedMb} MB</strong></header><i><b style={{ width: memoryWidth }} /></i><small>{data.resourceLimits.memoryCapMb} MB cap · cgroup bounded</small></div></section><section className="sandbox-flags"><span className={data.isolationFlags.networkDisabled ? "is-safe" : "is-warning"}><Network size={13} />Network: {data.isolationFlags.networkDisabled ? "disabled" : "attached"}</span><span className={data.isolationFlags.readOnlyHostMount ? "is-safe" : "is-warning"}><LockKeyhole size={13} />Host mount: {data.isolationFlags.readOnlyHostMount ? "read-only" : "read-write"}</span></section>{data.executionTestResult && <section className={`sandbox-result ${data.executionTestResult.exitCode === 0 ? "is-success" : "is-failed"}`}><b>EXIT {data.executionTestResult.exitCode}</b><p>{data.executionTestResult.outputDiffSummary}</p></section>}</div></section>;
}
